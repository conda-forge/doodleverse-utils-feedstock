{% set python_min = "3.7" %}
{% set name = "doodleverse-utils" %}
{% set version = "0.0.37" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/doodleverse-utils-{{ version }}.tar.gz
  sha256: b423118a79df5bff75666003a343430dd5b7a8883d5855113295838618bdd267

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - doodleverse_utils
  commands:
    # bogus requirement on versioneer
    # - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/Doodleverse/doodleverse_utils
  summary: Imports into the Doodleverse
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ocefpaf
    - 2320sharon
